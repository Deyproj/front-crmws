# URL interna del backend api-crmws para los route handlers server-side (app/api/**),
# que actúan como proxy entre el navegador y el backend real. No se expone al cliente.
# Coincide con el puerto por defecto de api-crmws (docs/00-getting-started.md).
API_INTERNAL_BASE_URL=http://localhost:8080/api/v1
